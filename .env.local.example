# Site URL (프로덕션: https://yourdomain.com)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Database (Supabase PostgreSQL)
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres

# OAuth (Supabase Dashboard에서 설정 필요)
# 카카오: https://developers.kakao.com/
# 구글: https://console.cloud.google.com/
# Callback URL: https://your-project.supabase.co/auth/v1/callback

# Anthropic Claude API (선택)
ANTHROPIC_API_KEY=sk-ant-...

# ============================================
# 7.4 PostHog 분석 설정
# ============================================
# PostHog: https://posthog.com/
# 1. PostHog 대시보드에서 프로젝트 생성
# 2. Settings → Project Settings → Copy API Key
# 3. API Host는 기본값(https://us.posthog.com) 또는 EU(https://eu.posthog.com)
NEXT_PUBLIC_POSTHOG_KEY=phc_your-api-key
NEXT_PUBLIC_POSTHOG_HOST=https://us.posthog.com

# ============================================
# 7.5 Sentry 에러 추적 설정
# ============================================
# Sentry: https://sentry.io/
# 1. Sentry 대시보드에서 프로젝트 생성 (Next.js)
# 2. Settings → Client Keys (DSN) → Copy DSN
# 3. DSN 형식: https://[key]@[domain].ingest.sentry.io/[project-id]
NEXT_PUBLIC_SENTRY_DSN=https://[key]@[domain].ingest.sentry.io/[project-id]
NEXT_PUBLIC_SENTRY_TRACES_SAMPLE_RATE=0.1

# 앱 버전 (분석에 포함)
NEXT_PUBLIC_APP_VERSION=0.1.0

# ============================================
# 6.8 Upstash Redis 캐싱 설정
# ============================================
# Upstash: https://upstash.com/
# 1. Upstash Console에서 Redis 데이터베이스 생성
# 2. Details → REST API 섹션에서 URL과 Token 복사
# 3. UPSTASH_REDIS_REST_URL: https://[endpoint].upstash.io
# 4. UPSTASH_REDIS_REST_TOKEN: [your-rest-token]
UPSTASH_REDIS_REST_URL=https://[endpoint].upstash.io
UPSTASH_REDIS_REST_TOKEN=your-rest-token

# ============================================
# 6.5 이메일 알림 설정 (Resend)
# ============================================
# Resend: https://resend.com/
# 1. Resend 대시보드에서 API Key 생성
# 2. Settings → API Keys → Create API Key
# 3. 발신자 이메일 도메인 인증 필수 (DNS 레코드 추가)
RESEND_API_KEY=re_your-api-key
RESEND_FROM_EMAIL=noreply@yourdomain.com
RESEND_FROM_NAME=FlowStok

# ============================================
# 6.6 SMS 알림 설정 (CoolSMS)
# ============================================
# CoolSMS: https://www.coolsms.co.kr/
# 1. CoolSMS 계정 생성 및 API Key 발급
# 2. API Key 관리 → API Key 생성
# 3. 발신번호 등록 필수 (통신사 인증)
COOLSMS_API_KEY=your-api-key
COOLSMS_API_SECRET=your-api-secret
COOLSMS_SENDER_PHONE=01012345678

# Mock 모드 (true면 실제 전송 안함, 로그만 출력)
NOTIFICATIONS_MOCK_MODE=true

# ============================================
# 7.1 PortOne + 토스페이먼츠 결제 설정
# ============================================
# PortOne: https://portone.io/
# 1. PortOne 콘솔에서 스토어 생성
# 2. 설정 → API/Webhook → Store ID 복사
# 3. 설정 → API 키 관리 → API Secret 생성
# 4. 토스페이먼츠 연동: 결제 → PG 연동 → 토스페이먼츠 설정
# 5. 웹훅 URL 등록: https://yourdomain.com/api/payment/webhook

# PortOne V2 설정
NEXT_PUBLIC_PORTONE_STORE_ID=store-your-store-id
NEXT_PUBLIC_PORTONE_CHANNEL_KEY=channel-key-your-channel-key
PORTONE_API_SECRET=your-api-secret
PORTONE_WEBHOOK_SECRET=your-webhook-secret

# 결제 Mock 모드 (개발/테스트 환경)
# true: 실제 결제 없이 시뮬레이션, false: 실제 결제
NEXT_PUBLIC_PAYMENT_MOCK=true

# ============================================
# 6.12 Railway 백그라운드 잡 설정
# ============================================
# Cron Secret (Railway 크론잡 인증 토큰)
# 생성: openssl rand -base64 32
# Railway 대시보드 → Settings → Variables에 추가
CRON_SECRET=your-random-secret-token-here
